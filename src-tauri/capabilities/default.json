{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permisos por defecto del envoltorio de escritorio.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
