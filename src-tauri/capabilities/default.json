{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permisos por defecto del envoltorio de escritorio.",
  "windows": ["main"],
  "remote": {
    "urls": ["https://os.labstreamsas.com"]
  },
  "permissions": [
    "core:default",
    "opener:default",
    "notification:default"
  ]
}
