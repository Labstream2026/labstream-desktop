{
  "name": "labstream-desktop",
  "version": "1.14.1",
  "private": true,
  "description": "App de escritorio (Windows) que envuelve Labstream OS — cliente del servidor del NAS.",
  "scripts": {
    "test": "node --test tests/images.test.cjs",
    "icons": "tauri icon app-icon.png",
    "dev": "npm run icons && tauri dev",
    "build": "npm run icons && tauri build",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
